generator client {
  provider        = "prisma-client"
  previewFeatures = ["relationJoins"]
  output          = "../app/generated/prisma"
  runtime         = "vercel-edge"
  engineType      = "client"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}
