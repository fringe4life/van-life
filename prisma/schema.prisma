generator client {
  provider        = "prisma-client"
  previewFeatures = ["relationJoins"]
  output          = "../app/generated/prisma"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}
