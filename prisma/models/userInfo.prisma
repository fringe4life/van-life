model UserInfo {
  userId     String @id
  user       User   @relation(fields: [userId], references: [id])
  moneyAdded Float  @default(0) @db.DoublePrecision

  vans Van[]

  renter Rent[] @relation("renter")
  rented Rent[] @relation("host")

  review Review[]

  @@index([userId])
}
