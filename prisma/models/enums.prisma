enum VanType {
  SIMPLE @map("simple")
  LUXURY @map("luxury")
  RUGGED @map("rugged")
}

enum TransactionType {
  DEPOSIT  @map("deposit")
  WITHDRAW @map("withdraw")
}

enum VanState {
  IN_REPAIR @map("in_repair")
  ON_SALE   @map("on_sale")
  AVAILABLE @map("available")
}
