import { account, session, user, verification } from "./auth";
import { rent, review, transaction, van } from "./van";

export const schema = {
  account,
  rent,
  review,
  session,
  transaction,
  user,
  van,
  verification,
};
